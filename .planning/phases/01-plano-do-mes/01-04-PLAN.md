---
phase: 01-plano-do-mes
plan: "04"
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - src/app/api/monthly-plan/route.ts
  - src/app/api/monthly-plan/route.test.ts
autonomous: true
requirements: [PMES-001, PMES-002, PMES-004, PMES-005, PMES-011]
must_haves:
  truths:
    - "Usuário autenticado consulta e atualiza somente seu plano do mês."
    - "PUT aceita apenas month, incomeOverride, savingsGoal e safetyMargin."
    - "GET e PUT retornam o mesmo DTO recalculado; erros não vazam Prisma."
  artifacts:
    - path: src/app/api/monthly-plan/route.ts
      provides: "GET/PUT autenticados e dinâmicos"
    - path: src/app/api/monthly-plan/route.test.ts
      provides: "Contrato HTTP, auth, validação e anti-mass-assignment"
  key_links:
    - from: src/app/api/monthly-plan/route.ts
      to: src/features/monthly-plan/monthly-plan.service.ts
      via: "session.user.id + entrada Zod"
---

<objective>
Expor o Plano do Mês por API autenticada e estrita.

Purpose: tornar criação/edição/leitura consumível por página e dashboard sem abrir BOLA ou mass assignment.
Output: Route Handler GET/PUT e testes HTTP.
</objective>

<context>
@AGENTS.md
@.planning/phases/01-plano-do-mes/01-CONTEXT.md
@src/app/api/budgets/route.ts
@src/lib/auth.ts
@node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| navegador→Route Handler | Cookie, query e JSON não confiáveis. |
| Route Handler→serviço tenant | Somente session.user.id pode selecionar tenant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-11 | Spoofing | GET/PUT | mitigate | `auth()` obrigatório; 401 sem session user id. |
| T-01-12 | Tampering | PUT | mitigate | Zod strict allowlist, rejeição de userId/id/derivados e origem cruzada quando presente. |
| T-01-13 | Information disclosure | resposta/erros | mitigate | Tenant da sessão, cache público proibido e mensagens genéricas em 5xx. |
</threat_model>

<tasks>

<task id="01-04-T01" type="auto" tdd="true">
  <name>Fixar contrato HTTP e segurança antes da rota</name>
  <files>src/app/api/monthly-plan/route.test.ts</files>
  <action>Antes de código, ler integralmente a documentação Next 16 local de Route Handlers indicada no contexto e respeitar deprecações. Escrever testes do contrato existente e da janela D-17: extremos inclusivos aceitos; mês anterior/posterior rejeitado 400 antes de mock/spy de materialização/serviço. Fixar relógio em São Paulo. Confirmar auth, allowlist, Origin, erros e cache. Análogos PATTERNS.md: `src/app/api/budgets/route.ts` para auth/safeParse/erros e `dashboard/summary/route.ts` para query autenticada.</action>
  <verify><automated>npm test -- src/app/api/monthly-plan/route.test.ts</automated></verify>
  <acceptance>Testes provam auth, allowlist, tenant derivado, cache privado/dinâmico, CSRF básico e respostas 200/400/401/5xx.</acceptance>
  <done>Contrato HTTP e ameaças da borda estão cobertos antes da rota.</done>
</task>

<task id="01-04-T02" type="auto">
  <name>Implementar GET/PUT mensal conforme padrões Next 16</name>
  <files>src/app/api/monthly-plan/route.ts</files>
  <action>Implementar Route Handler conforme docs locais e análogo budgets: auth, validação query/body, janela D-17 calculada com `asOf` em America/Sao_Paulo e rejeição antes do serviço, GET/PUT tenant-aware e projeção recalculada. Manter proteções de mass assignment/cache/Origin/erros já planejadas. Análogos PATTERNS.md: budgets route para controle e monthly-plan schema/service dos planos 02/03 para contrato.</action>
  <verify><automated>npm test -- src/app/api/monthly-plan/route.test.ts &amp;&amp; npm run lint</automated></verify>
  <acceptance>Rota passa contrato; outro tenant não é selecionável pelo cliente; GET/PUT usam fonte única e não expõem erro interno.</acceptance>
  <done>GET/PUT autenticados passam teste de rota e lint.</done>
</task>

</tasks>

<verification>
Executar teste da rota e lint; revisar tenant/cache/janela. Antes de qualquer commit/checkpoint da wave: `npm run test:all && npm run lint && npm run build`, per AGENTS.md.
</verification>

<success_criteria>
- API cria/edita/consulta plano mensal autenticado.
- Zero e null mantêm semântica de override.
- Mass assignment, BOLA, cache cruzado e erro detalhado estão cobertos.
</success_criteria>

<output>Após execução, criar `.planning/phases/01-plano-do-mes/01-04-SUMMARY.md`.</output>
