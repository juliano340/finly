---
phase: 01-plano-do-mes
plan: "08"
type: execute
wave: 6
depends_on: ["01-01", "01-07"]
files_modified:
  - scripts/vercel-build.mjs
  - scripts/verify-production-schema.mjs
  - src/__tests__/vercel-build.test.ts
  - CHANGELOG.md
  - docs/MIGRATIONS.md
  - docs/deployment-vercel-vps-postgres.md
autonomous: true
requirements: [PMES-001, PMES-003, PMES-011]
must_haves:
  truths:
    - "Build de produção falha fechado sem MIGRATE_DATABASE_URL."
    - "migrate deploy e smoke terminam antes de next build."
    - "Smoke distingue DML, CREATE e capacidade de ALTER corretamente."
    - "Runbooks documentam Transaction avulsa e rollout seguro."
  artifacts:
    - path: scripts/vercel-build.mjs
      provides: "Orquestração migration→smoke→build"
    - path: scripts/verify-production-schema.mjs
      provides: "Smoke PostgreSQL estrutural/permissões"
    - path: src/__tests__/vercel-build.test.ts
      provides: "Teste fail-closed e ordem"
  key_links:
    - from: scripts/vercel-build.mjs
      to: scripts/verify-production-schema.mjs
      via: "após migrate deploy e antes de next build"
---

<objective>
Tornar rollout do MonthlyPlan fail-closed e verificável.

Purpose: impedir aplicação nova contra schema/permissões ausentes.
Output: pipeline testado, smoke PostgreSQL, changelog e runbooks.
</objective>

<context>
@AGENTS.md
@scripts/vercel-build.mjs
@scripts/migrate-production.mjs
@prisma/migrations/20260809180000_add_monthly_plan/migration.sql
@docs/MIGRATIONS.md
@docs/deployment-vercel-vps-postgres.md
@CHANGELOG.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Vercel build→PostgreSQL | Deploy só continua após schema/grants comprovados. |
| migrator→runtime roles | Verificação privilegiada não concede DDL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-23 | Denial of service | vercel-build | mitigate | Fail closed e ordem testada. |
| T-01-24 | Elevation of privilege | smoke roles | mitigate | DML por table privilege; CREATE por schema privilege; ALTER por owner/membership. |
| T-01-25 | Information disclosure | env/docs | mitigate | URL somente no subprocesso; nenhum segredo em logs/docs. |
</threat_model>

<tasks>

<task id="01-08-T01" type="auto" tdd="true">
  <name>Fixar comportamento fail-closed do build</name>
  <files>src/__tests__/vercel-build.test.ts</files>
  <action>Escrever Vitest sob `src/__tests__` com `@vitest-environment node`, mockando `spawnSync`: produção sem/blank MIGRATE_DATABASE_URL falha e não chama Next; sucesso chama migrate, smoke, Next nessa ordem; falha de migrate/smoke interrompe; não-produção mantém build. Não imprimir URL. Análogos PATTERNS.md/projeto: `vercel-build.mjs` para spawn fail-fast e `migrate-production.mjs` para DATABASE_URL restrita ao subprocesso.</action>
  <verify><automated>npm test -- src/__tests__/vercel-build.test.ts</automated></verify>
  <acceptance>Testes RED cobrem ausência, ordem e propagação.</acceptance>
  <done>Contrato pipeline está executável antes da implementação.</done>
</task>

<task id="01-08-T02" type="auto">
  <name>Implementar smoke, pipeline e documentação migration-first</name>
  <files>scripts/vercel-build.mjs, scripts/verify-production-schema.mjs, CHANGELOG.md, docs/MIGRATIONS.md, docs/deployment-vercel-vps-postgres.md</files>
  <action>Em produção, exigir MIGRATE_DATABASE_URL, rodar migrate deploy, smoke via `pg` e só então Next build. Smoke falha se MonthlyPlan/FK/unique/migration faltam; para ambos runtimes, `has_table_privilege` somente DML, `has_schema_privilege(...,'CREATE')` falso, e owner pg_class + `pg_has_role(runtime,owner,'MEMBER')` falso para incapacidade de ALTER. Atualizar changelog/runbooks com D-16, limitação manual/proveniência fora do escopo, D-17, teste PostgreSQL efêmero e ordem backup/status→migrate→smoke→build/deploy→smoke funcional. Não editar migration aplicada, vazar segredo ou apagar tabela no rollback. Análogos PATTERNS.md/projeto: migrations quoted, dual schema, `vercel-build.mjs` e seção Vercel de MIGRATIONS.</action>
  <verify><automated>npm test -- src/__tests__/vercel-build.test.ts src/__tests__/monthly-plan.postgres.test.ts &amp;&amp; npm run prisma:generate &amp;&amp; npm run test:coverage &amp;&amp; npm run test:all &amp;&amp; npm run lint &amp;&amp; npm run build</automated></verify>
  <acceptance>Produção não alcança build sem credencial/migration/smoke; checks são corretos; docs refletem rollout.</acceptance>
  <done>Pipeline, smoke, docs e gate completo ficam verdes.</done>
</task>

</tasks>

<verification>
Antes de qualquer commit/checkpoint executar `npm run test:all && npm run lint && npm run build`; T02 acrescenta coverage e testes operacionais. Nenhum gate parcial.
</verification>

<success_criteria>
- Produção falha fechado sem MIGRATE_DATABASE_URL.
- Ordem migrate→smoke→build é testada.
- Smoke valida estrutura, DML, CREATE e ALTER corretamente.
- Changelog/docs cobrem D-16/D-17 e rollout.
</success_criteria>

<output>Após execução, criar `.planning/phases/01-plano-do-mes/01-08-SUMMARY.md`.</output>
