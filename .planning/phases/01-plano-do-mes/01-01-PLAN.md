---
phase: 01-plano-do-mes
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/schema.sqlite.prisma
  - prisma/migrations/20260809180000_add_monthly_plan/migration.sql
  - src/__tests__/schema.test.ts
  - src/__tests__/relations.test.ts
  - src/__tests__/monthly-plan.postgres.test.ts
autonomous: true
requirements: [PMES-001, PMES-004, PMES-005, PMES-011]
must_haves:
  truths:
    - "Cada usuário pode ter no máximo um MonthlyPlan por mês."
    - "Meta, margem e override são persistidos; projeções e limites não são."
    - "PostgreSQL e SQLite expõem o mesmo contrato de modelo."
    - "finly_app e finly_runtime recebem DML operacional, sem CREATE no schema nem ownership/membership que permita ALTER."
  artifacts:
    - path: prisma/schema.prisma
      provides: "Modelo MonthlyPlan PostgreSQL com Decimal(19,2)"
    - path: prisma/schema.sqlite.prisma
      provides: "Espelho SQLite do modelo MonthlyPlan"
    - path: prisma/migrations/20260809180000_add_monthly_plan/migration.sql
      provides: "DDL PostgreSQL e grants runtime aditivos"
  key_links:
    - from: prisma/schema.prisma
      to: prisma/migrations/20260809180000_add_monthly_plan/migration.sql
      via: "tabela, FK cascade e unique userId+month equivalentes"
---

<objective>
Criar persistência isolada do Plano do Mês em entidade própria.

Purpose: implementar D-14 e a base de PMES-001/004/005/011 sem alterar migration aplicada nem armazenar valores derivados.
Output: schemas duais, nova migration PostgreSQL, permissões runtime e testes estruturais.
</objective>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-plano-do-mes/01-CONTEXT.md
@.planning/phases/01-plano-do-mes/01-PATTERNS.md
@prisma/schema.prisma
@prisma/schema.sqlite.prisma
@docs/MIGRATIONS.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| aplicação→PostgreSQL | Role runtime deve operar dados sem poder alterar schema. |
| tenant→linha mensal | Constraint e FK devem preservar identidade usuário+mês. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-01 | Tampering | migration/schema | mitigate | `@@unique([userId, month])`, FK cascade e campos derivados ausentes. |
| T-01-02 | Elevation of privilege | grants PostgreSQL | mitigate | Nova migration concede apenas SELECT/INSERT/UPDATE/DELETE a `finly_app` e `finly_runtime`, condicionalmente à existência do role. |
| T-01-03 | Information disclosure | MonthlyPlan | mitigate | Relação obrigatória com User; consultas tenant serão testadas no plano 03. |
</threat_model>

<tasks>

<task id="01-01-T01" type="auto" tdd="true">
  <name>Definir primeiro os testes do contrato persistente</name>
  <files>src/__tests__/schema.test.ts, src/__tests__/relations.test.ts, src/__tests__/monthly-plan.postgres.test.ts</files>
  <action>Antes dos schemas, adicionar testes que falhem pela ausência de `MonthlyPlan`: relação `User.monthlyPlans`, campos, cascade, unique composto, paridade Decimal e ausência de derivados per D-14. Criar também teste PostgreSQL efêmero obrigatório com `@vitest-environment node` e timeout explícito adequado: iniciar container Docker isolado, criar `finly_owner`, `finly_migrator`, `finly_app` e `finly_runtime`, aplicar migrations como migrator e encerrar/remover container em `finally`. Após aplicação, consultar catálogo para tabela/FK/unique; usar `has_table_privilege` apenas para SELECT/INSERT/UPDATE/DELETE; usar `has_schema_privilege(role,'public','CREATE')` para provar CREATE negado; obter owner de `pg_class` e usar igualdade/`pg_has_role(role,owner,'MEMBER')` para provar que runtimes não podem ALTER por ownership/membership. Confirmar RED com falha esperada causada somente pela migration `20260809180000_add_monthly_plan` ainda ausente; o container deve ser removido em `finally` mesmo nesse estado RED. Análogos de PATTERNS.md: `schema.test.ts`, `relations.test.ts`, migration init PostgreSQL e `20260809143000_use_decimal_for_money`.</action>
  <verify><automated>npm test -- src/__tests__/schema.test.ts src/__tests__/relations.test.ts src/__tests__/monthly-plan.postgres.test.ts</automated></verify>
  <acceptance>Testes descrevem paridade dual, unicidade mensal, cascade e ausência de derivados; inicialmente falham somente porque o modelo ainda não existe.</acceptance>
  <done>Contrato de persistência está expresso em testes executáveis antes da produção.</done>
</task>

<task id="01-01-T02" type="auto">
  <name>Adicionar MonthlyPlan aos dois schemas e migration aditiva</name>
  <files>prisma/schema.prisma, prisma/schema.sqlite.prisma, prisma/migrations/20260809180000_add_monthly_plan/migration.sql</files>
  <action>Implementar entidade separada `MonthlyPlan` per D-14 usando contrato já descrito; PostgreSQL usa Decimal(19,2), SQLite Decimal. Criar somente migration nova. Se `finly_owner` existir, transferir ownership da tabela para ele; para cada runtime existente, revogar CREATE no schema public e conceder somente DML na tabela. Não tentar provar ALTER com `has_table_privilege`: impedir por owner/membership e validar no teste. Blocos condicionais não falham em bancos locais sem roles. Não persistir derivados; gerar ambos clients. Análogos PATTERNS.md: `FinancialMonth` para identidade/cascade, `Budget` para Decimal e migration `20260809143000_use_decimal_for_money` para SQL quoted.</action>
  <verify><automated>npx prisma validate --schema prisma/schema.prisma &amp;&amp; npx prisma validate --schema prisma/schema.sqlite.prisma &amp;&amp; npm run prisma:generate &amp;&amp; npm test -- src/__tests__/schema.test.ts src/__tests__/relations.test.ts src/__tests__/monthly-plan.postgres.test.ts</automated></verify>
  <acceptance>Schemas equivalentes geram; testes ficam verdes; migration nova contém tabela/FK/unique e grants DML condicionais para ambos roles, sem mudança em migration aplicada.</acceptance>
  <done>Modelo dual e migration aditiva passam validação, geração e testes estruturais.</done>
</task>

</tasks>

<verification>
Executar validação Prisma dupla, geração, testes SQLite/PostgreSQL efêmero. Antes de qualquer commit/checkpoint da wave: `npm run test:all && npm run lint && npm run build`, per AGENTS.md. Confirmar que nenhuma migration anterior mudou.
</verification>

<success_criteria>
- MonthlyPlan separado e único por `userId+month` existe nos dois schemas.
- Dinheiro usa Decimal coerente em produção/testes.
- Nova tabela recebe acesso operacional de `finly_app` e `finly_runtime` sem ampliar DDL.
- PMES-001/004/005/011 têm fundação persistente testada.
</success_criteria>

<output>Após execução, criar `.planning/phases/01-plano-do-mes/01-01-SUMMARY.md`.</output>
